# CPU-only PyTorch wheels. Without this, sentence-transformers pulls the
# default GPU build of torch, which drags in ~2GB of nvidia-* CUDA packages
# nobody needs here (this project runs entirely on CPU + Ollama). That extra
# 2GB of downloads over a slow connection is what caused the BrokenPipeError
# failures during the build.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Multi-agent orchestration
crewai==0.83.0
crewai-tools==0.76.0
pydantic-ai==0.0.14

# --- Pinned transitive deps of crewai / pydantic-ai ---
# crewai declares these as loose ranges (e.g. "litellm>=1.44.22"), which
# forces pip to download and inspect metadata for hundreds of releases one
# by one before it finds a compatible set ("pip is looking at multiple
# versions..."). That backtracking alone was eating 10-20+ minutes per
# build even when nothing failed. Pinning exact known-good versions skips
# all of that. Do not remove these pins.
litellm==1.53.3
mistralai==1.9.11
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-proto==1.27.0
opentelemetry-semantic-conventions==0.48b0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-instrumentation==0.48b0
opentelemetry-instrumentation-asgi==0.48b0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-util-http==0.48b0
protobuf==4.25.9
grpcio==1.83.0
importlib-metadata==8.4.0
jsonschema==4.26.0

# Vector DB / knowledge base
chromadb==0.5.20
sentence-transformers==3.2.1
huggingface-hub==0.36.2
transformers==4.57.6
tokenizers==0.22.2

# CPU-only torch (resolved from the --extra-index-url above)
torch==2.4.1+cpu

# Vision & OCR
opencv-python-headless==4.10.0.84
tensorflow-cpu==2.17.0
tf-keras==2.17.0

# Data processing / synthetic data
pandas==2.2.2
numpy==1.26.4
faker==28.4.1

# LLM client for the copilot's direct streaming calls to Ollama
openai==1.54.3
pyyaml==6.0.2
